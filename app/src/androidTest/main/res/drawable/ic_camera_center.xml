<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Center the camera vector inside the FAB -->
    <item
        android:gravity="center">
        <!-- If you still see a slight drift, uncomment and tweak the inset values below -->
        <!--
        <inset
            android:insetLeft="0dp"
            android:insetTop="1dp"
            android:insetRight="0dp"
            android:insetBottom="0dp">
            <bitmap android:src="@drawable/ic_camera" />
        </inset>
        -->
        <!-- Usually this is enough (no inset): -->
        <bitmap android:src="@drawable/ic_camera" />
    </item>
</layer-list>
